# ScheduleSolver v2
# No required environment variables -- the app runs with zero configuration.
# SQLite database and XGBoost model are auto-generated on first run.

# Optional: Ollama configuration (for AI Agent feature)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=qwen2.5:7b
